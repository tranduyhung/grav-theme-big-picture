<header id="header">
    <h1 id="logo"><a href="{{ base_url == '' ? '/' : base_url }}">{{ site.title }}</a></h1>
    <nav id="nav">
        <ul>
            {% if page.template() != 'modular' %}
              <li><a href="{{ base_url == '' ? '/' : base_url }}">{{ page.menu }}</a></li>
            {% endif %}
            {% for module in page.collection() %}
            <li><a href="#{{ module.folder|replace({'_': ''}) }}">{{ module.menu }}</a></li>
            {% endfor %}
        </ul>
    </nav>
</header>
